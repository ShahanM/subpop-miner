def PreprocessData(data, config):
	"""Preprocess data for training and testing

	Args:
		data (pd.DataFrame): data for training and testing
		config (dict): config for preprocessing

	Returns:
		pd.DataFrame: preprocessed data
	"""

	return data
